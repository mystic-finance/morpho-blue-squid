services:
  db_flare:
    image: postgres:15
    restart: unless-stopped
    environment:
      POSTGRES_DB: morpho_flare
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgresflarepassword
    ports: ["5432:5432"]
    volumes:
      - db_flare_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres"]
      interval: 5s
      timeout: 5s
      retries: 10

  processor_flare:
    build: .
    restart: unless-stopped
    env_file: .env.flare
    environment:
      DB_HOST: db_flare
    depends_on:
      db_flare:
        condition: service_healthy
    volumes:
      - ./oracle-feeds.json:/squid/oracle-feeds.json
    command: sh -c "npx squid-typeorm-migration apply && node lib/main.js"

  graphql_flare:
    build: .
    restart: unless-stopped
    env_file: .env.flare
    environment:
      DB_HOST: db_flare
    depends_on:
      db_flare:
        condition: service_healthy
    command: npx squid-graphql-server
    ports: ["4350:4000"]

  db_plume:
    image: postgres:15
    restart: unless-stopped
    environment:
      POSTGRES_DB: morpho_plume
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgresplumepassword
    ports: ["5433:5432"]
    volumes:
      - db_plume_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres"]
      interval: 5s
      timeout: 5s
      retries: 10

  processor_plume:
    build: .
    restart: unless-stopped
    env_file: .env.plume
    environment:
      DB_HOST: db_plume
    depends_on:
      db_plume:
        condition: service_healthy
    volumes:
      - ./oracle-feeds.json:/squid/oracle-feeds.json
    command: sh -c "npx squid-typeorm-migration apply && node lib/main.js"

  graphql_plume:
    build: .
    restart: unless-stopped
    env_file: .env.plume
    environment:
      DB_HOST: db_plume
    depends_on:
      db_plume:
        condition: service_healthy
    command: npx squid-graphql-server
    ports: ["4351:4000"]

  db_citrea:
    image: postgres:15
    restart: unless-stopped
    environment:
      POSTGRES_DB: morpho_citrea
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgrescitreapassword
    ports: ["5434:5432"]
    volumes:
      - db_citrea_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres"]
      interval: 5s
      timeout: 5s
      retries: 10

  processor_citrea:
    build: .
    restart: unless-stopped
    env_file: .env.citrea
    environment:
      DB_HOST: db_citrea
    depends_on:
      db_citrea:
        condition: service_healthy
    volumes:
      - ./oracle-feeds.json:/squid/oracle-feeds.json
    command: sh -c "npx squid-typeorm-migration apply && node lib/main.js"

  graphql_citrea:
    build: .
    restart: unless-stopped
    env_file: .env.citrea
    environment:
      DB_HOST: db_citrea
    depends_on:
      db_citrea:
        condition: service_healthy
    command: npx squid-graphql-server
    ports: ["4352:4000"]

  db_canton:
    image: postgres:15
    restart: unless-stopped
    environment:
      POSTGRES_DB: morpho_canton
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgrescantonpassword
    ports: ["5435:5432"]
    volumes:
      - db_canton_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres"]
      interval: 5s
      timeout: 5s
      retries: 10

  processor_canton:
    build: .
    restart: unless-stopped
    env_file: .env.canton
    environment:
      DB_HOST: db_canton
    depends_on:
      db_canton:
        condition: service_healthy
    command: sh -c "npx squid-typeorm-migration apply && node lib/main.js"

  graphql_canton:
    build: .
    restart: unless-stopped
    env_file: .env.canton
    environment:
      DB_HOST: db_canton
    depends_on:
      db_canton:
        condition: service_healthy
    command: npx squid-graphql-server
    ports: ["4353:4000"]

volumes:
  db_flare_data:
  db_plume_data:
  db_citrea_data:
  db_canton_data:
